# Terraform local state (using local state for demo — don't commit)
*.tfstate
*.tfstate.*

# Terraform working files
.terraform/
.terraform.lock.hcl
*.tfplan
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Sensitive variable files (if any contain secrets)
# *.auto.tfvars
